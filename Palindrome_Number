bool isPalindrome1(int x) 
{
  int x_copy = x;
  int reversion_num = 0;
  while ( x_copy > 0)
  {
    reversion_num = reversion_num * 10 + x_copy%10;
    x_copy = x_copy/10;
  };

  if ( x == reversion_num) {
    return true;
  }

  return false;
}
  
